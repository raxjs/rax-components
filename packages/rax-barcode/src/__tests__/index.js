import {createElement, Component} from 'rax';
import renderer from 'rax-test-renderer';
import BarCode from '../';

describe('BarCode', () => {
  it('test typeof BarCode', () => {
    expect(typeof BarCode).toEqual('function');
  });
});
