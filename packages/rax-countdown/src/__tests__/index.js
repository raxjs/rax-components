import {createElement, Component} from 'rax';
import renderer from 'rax-test-renderer';
import CountDown from '../';

describe('CountDown', () => {
  it('test typeof CountDown', () => {
    expect(typeof CountDown).toEqual('function');
  });
});
