import {createElement, Component} from 'rax';
import renderer from 'rax-test-renderer';
import Datepicker from '../';

describe('Datepicker', () => {
  it('test typeof Datepicker', () => {
    expect(typeof Datepicker).toEqual('function');
  });
});
