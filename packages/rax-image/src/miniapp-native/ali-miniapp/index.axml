<image
  style="{{styleSheet}}" 
  class="{{className}}"
  src="{{source.uri}}"
  default-source="{{placeholder}}"
  onTap="onClick"
  onError="onError"
  onLoad="onLoad"
  mode="{{mode}}"
  lazy-load="{{lazyLoad || loading == 'lazy'}}"
/>
