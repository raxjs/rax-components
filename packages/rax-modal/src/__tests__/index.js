/* eslint-env jest */
import {
  createElement
} from 'rax';
import renderer from 'rax-test-renderer';
import RaxModal from '../';

describe('RaxModal', () => {
  it('typeof RaxModal is "function"', () => {
    expect(typeof RaxModal).toEqual('function');
  });
});
