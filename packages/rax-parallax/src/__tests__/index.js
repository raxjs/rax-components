import {createElement, Component} from 'rax';
import renderer from 'rax-test-renderer';
import Parallax from '../';

describe('Parallax', () => {
  it('test typeof Parallax', () => {
    expect(typeof Parallax).toEqual('function');
  });
});
