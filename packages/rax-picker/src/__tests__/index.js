import {createElement, Component} from 'rax';
import renderer from 'rax-test-renderer';
import Picker from '../';

describe('Picker', () => {
  it('test typeof Picker', () => {
    expect(typeof Picker).toEqual('function');
  });
});
