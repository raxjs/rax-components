import {createElement, Component} from 'rax';
import renderer from 'rax-test-renderer';
import Pictrue from '../../lib/';

describe('Pictrue', () => {
  it('test typeof Pictrue', () => {
    expect(typeof Pictrue).toEqual('function');
  });
});
