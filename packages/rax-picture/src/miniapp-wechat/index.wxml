<rax-image
    style="{{styleSheet}}" 
    class="{{className}}"
    source="{{source}}"
    onClick="onClick"
    onError="onError"
    onLoad="onLoad"
    resizeMode="{{resizeMode}}"
    lazyLoad="{{lazyload}}"
/>
