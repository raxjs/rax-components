<rax-image
    styleSheet="{{styleSheet}}" 
    className="{{className}}"
    source="{{source}}"
    bindonClick="onClick"
    bindonError="onError"
    bindonLoad="onLoad"
    resizeMode="{{resizeMode}}"
    lazyLoad="{{lazyload}}"
    mode="{{mode}}"
/>
