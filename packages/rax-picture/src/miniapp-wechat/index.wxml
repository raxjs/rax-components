<rax-image
    styleSheet="{{styleSheet}}" 
    class="{{className}}"
    source="{{source}}"
    bindonClick="onClick"
    bindonError="onError"
    bindonLoad="onLoad"
    resizeMode="{{resizeMode}}"
    lazyLoad="{{lazyload}}"
/>
