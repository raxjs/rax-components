import * as Rax from 'rax';

const styles: {
  [key: string]: Rax.CSSProperties;
} = {
  initial: {
    border: '0 solid black',
    boxSizing: 'border-box',
    display: 'flex',
    flexDirection: 'column',
    alignContent: 'flex-start',
    flexShrink: 0
  }
};

export default styles;