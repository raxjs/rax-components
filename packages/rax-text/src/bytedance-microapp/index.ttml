<text class="rax-text-v2 {{className}} {{numberOfLines ? '_mutilple-lines' : ''}}"
    style="-webkit-line-clamp: {{numberOfLines + 1}};{{styleSheet}}"
    number-of-lines="{{numberOfLines}}"
    selectable="{{selectable}}"
    space="{{space}}"
    decode="{{decode}}"
    onTap="onClick">
    <slot />
</text>
