import {createElement, Component} from 'rax';
import renderer from 'rax-test-renderer';
import Timepicker from '../';

describe('Timepicker', () => {
  it('test typeof Timepicker', () => {
    expect(typeof Timepicker).toEqual('function');
  });
});
