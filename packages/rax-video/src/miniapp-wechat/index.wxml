<video
    src="{{src}}" 
    class="{{className}}"
    controls="{{controls}}"
    autoplay="{{autoPlay}}"
    style="{{style}}" 
    loop="{{loop}}"
    bindtap="onClick"
    bindended="onEnded">
</video>
