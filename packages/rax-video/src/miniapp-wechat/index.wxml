<video
    src="{{src}}" 
    class="{{className}}"
    muted="{{muted}}"
    controls="{{controls}}"
    autoplay="{{autoPlay}}"
    style="{{style}}" 
    loop="{{loop}}"
    bindtap="onClick"
    bindended="onEnded">
</video>
