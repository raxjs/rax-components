<video
    id="{{id}}"
    src="{{src}}" 
    class="{{className}}"
    muted="{{muted}}"
    controls="{{controls}}"
    autoplay="{{autoPlay}}"
    style="{{style}}" 
    loop="{{loop}}"
    onTap="onClick"
    poster="{{poster}}"
    extraInfo="{{extraInfo}}"
    onEnded="onEnded">
</video>
